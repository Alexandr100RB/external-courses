function showAllProperties(obj) {
    for (let key in obj) {
        if (obj.hasOwnProperty(property)) {
            console.log(`Ключ: ${key}, свойство: ${obj[key]}`);
        }
    }

    return;
}

module.exports = showAllProperties();